import { EventEmitter } from 'shared/lib';
import { CellsEmitterData, CellsEventNames } from './interface';

export class CellsEventEmitter extends EventEmitter<CellsEmitterData, CellsEventNames> {
  // ? Преобразование в поток?
}
